<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>{% if title %}{{ title }} | {% endif %}{{ site.title }}</title>
  <meta name="description" content="{{ description or site.description }}">

  <!-- OpenGraph -->
  <meta property="og:title" content="{{ title or site.title }}">
  <meta property="og:description" content="{{ description or site.description }}">
  <meta property="og:url" content="{{ page.url | htmlBaseUrl(site.url) }}">
  <meta property="og:site_name" content="{{ site.title }}">
  <meta property="og:type" content="{% if layout == 'post.njk' %}article{% else %}website{% endif %}">
  {% if charts %}<meta property="og:image" content="{{ site.url }}{% chartImageUrl charts | firstChartId %}">{% endif %}

  <!-- Twitter Card -->
  <meta name="twitter:card" content="{% if charts %}summary_large_image{% else %}summary{% endif %}">
  <meta name="twitter:title" content="{{ title or site.title }}">
  <meta name="twitter:description" content="{{ description or site.description }}">
  {% if charts %}<meta name="twitter:image" content="{{ site.url }}{% chartImageUrl charts | firstChartId %}">{% endif %}

  <link rel="alternate" href="/feed.xml" type="application/atom+xml" title="{{ site.title }}">
  <link rel="alternate" href="/feed.json" type="application/json" title="{{ site.title }}">

  {% if site.author.mastodon_handle %}<meta name="fediverse:creator" content="{{ site.author.mastodon_handle }}">{% endif %}
  {% if site.author.mastodon_url %}<link rel="me" href="{{ site.author.mastodon_url }}">{% endif %}
  {% if site.author.bluesky_url %}<link rel="me" href="{{ site.author.bluesky_url }}">{% endif %}

  <link rel="stylesheet" href="/css/style.css">
  <link rel="stylesheet" href="https://kit.fontawesome.com/32d5416a89.css" crossorigin="anonymous">

  <script src="https://tinylytics.app/embed/Sdms2fWaFDoZZJx1ZuTe.js" defer></script>
</head>
<body>
  <header class="site-header">
    <div class="site-header__inner">
      <a href="/" class="site-title">{{ site.title }}</a>
      <nav class="site-nav">
        {%- for entry in nav %}
        <a href="{{ entry.url }}"{% if entry.url == page.url or entry.url + '/' == page.url %} aria-current="page"{% endif %}>{{ entry.title }}</a>
        {%- endfor %}
      </nav>
    </div>
  </header>

  <main class="site-main">
    {{ content | safe }}
  </main>

  <footer class="site-footer">
    <p>
      &copy; {% currentYear %} {{ site.author.name }} ·
      <a href="{{ site.repo }}">GitHub</a> ·
      <a href="https://github.com/rfordatascience/tidytuesday">TidyTuesday</a>
    </p>
    <p>Made with <a href="https://www.11ty.dev/">Eleventy</a> and <a href="https://uncharted.seanlunsford.com/">Uncharted</a></p>
  </footer>
</body>
</html>
